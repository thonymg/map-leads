# Configuration générée automatiquement
# Site: https://www.linkedin.com/feed/
# Généré le: 2026-02-24T19:36:52.416Z

name: linkedin-auth-scraper
url: ${LINKEDIN_URL}
headless: false

# Configuration de session
session:
  enabled: true
  name: linkedin_session
  file: /Users/anthonymichel/code/2026/Scrapper/map-leads/sessions/linkedin_session.json
  validate: true

scrapers:
  - name: linkedin-scraper
    url: ${LINKEDIN_URL}
    steps:
      # Charger la session
      - action: session-load
        params:
          sessionName: linkedin_session
          sessionsDir: ./sessions
      
      # Navigation vers la page protégée
      - action: navigate
        params:
          url: ${LINKEDIN_URL}
          timeout: 30000
      
      # Attendre le chargement
      - action: wait
        params:
          selector: body
          timeout: 10000
      
      # TODO: Ajoutez vos étapes d'extraction ici
      - action: extract
        params:
          selector: body
          fields:
            - name: content
              selector: body
